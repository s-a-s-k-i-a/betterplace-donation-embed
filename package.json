{
  "name": "betterplace-donation-embed",
  "version": "0.1.0",
  "description": "Tooling-Wrapper für E2E-Tests des betterplace-donation-embed WordPress-Plugins.",
  "private": true,
  "license": "GPL-2.0-or-later",
  "scripts": {
    "wp-env": "wp-env",
    "wp-env:start": "wp-env start",
    "wp-env:stop": "wp-env stop",
    "wp-env:clean": "wp-env clean all",
    "test:e2e": "cd tests/e2e && npm test",
    "test:e2e:install": "cd tests/e2e && npm install && npx playwright install --with-deps chromium",
    "test:smoke": "bash tests/smoke/smoke.sh"
  },
  "devDependencies": {
    "@wordpress/env": "^10.0.0"
  }
}
